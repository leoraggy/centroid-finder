1. In video summary app there should be a range between the threshold, for example check if its greater than 0 and less than 100?
2. Currently theres a bug that saves every frame of the image, I should remove the code. And perhaps only have 1.
3. It currently just saves directly to the loca filesystem how can I make it so that the serer outputs a downloadable csv file instead?
4. In DfsGroupFinder.java line 45 we check that image[row][col] is either 1 or 0 we throw NullPointerExpection but technically it isnt correct.

5.
6.
7.
8.
